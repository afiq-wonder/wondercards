import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.wonderlabs.wondercards",
  appName: "WonderCards",
  webDir: "mobile/out",
};

export default config;
